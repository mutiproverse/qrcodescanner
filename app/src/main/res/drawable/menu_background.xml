<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Background color of the menu -->
    <corners android:radius="26dp" /> <!-- Adjust the radius as needed -->
    <stroke
        android:width="1dp"
        android:color="@color/transparent" /> <!-- Optional: Add a border -->
</shape>